import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ionic.learnmagiciovue',
  appName: 'learnmagiciovue',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
